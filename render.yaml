services:
  - type: web
    name: ecard-backend
    env: python
    buildCommand: ./build.sh
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
    plan: free
    envVars:
      - key: PORT
        value: 10000
